{
  "name": "@ai-monitoring/shared",
  "version": "1.0.0",
  "description": "Shared types and interfaces for AI Monitoring Dashboard",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
